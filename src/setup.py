import setuptools

setuptools.setup(name='robo.motion',
    version='0.1',
    description='Robotic motions',
    url='#',
    author='brogrammer',
    install_requires=['numpy'],
    author_email='',
    packages=setuptools.find_packages(),
    zip_safe=False)